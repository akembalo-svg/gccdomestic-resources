source "https://rubygems.org"

# Mirrors the GitHub Pages build environment (pins Jekyll + supported
# plugins to the same versions GitHub Pages uses) so the CI build test
# matches production.
gem "github-pages", group: :jekyll_plugins
